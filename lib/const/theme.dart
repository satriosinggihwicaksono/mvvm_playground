import 'package:flutter/cupertino.dart';

const primaryColor = Color(0xFF0078d4);

const mainTheme = CupertinoThemeData(
    primaryColor: primaryColor,
    applyThemeToAll: true,
    brightness: Brightness.dark);
